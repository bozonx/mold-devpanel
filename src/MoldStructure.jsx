import React, { PropTypes } from 'react';

export default class MoldStructure extends React.Component {
  constructor(params) {
    super(params);
  }

  render() {
    return (
      <div>
        mold
      </div>
    );
  }
}
